const upcomingPage = () => {
  const element = document.createElement('div');
  element.id = 'upcoming-page';
  element.innerHTML = 'This is the upcoming page';

  return element;
};

export default upcomingPage;
