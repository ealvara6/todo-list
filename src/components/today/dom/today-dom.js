const todayPage = () => {
  const element = document.createElement('div');
  element.id = 'today-page';

  element.innerHTML = 'this is the today page.';

  return element;
};

export default todayPage;
